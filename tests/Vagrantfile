Vagrant.configure(2) do |config|

  # config.dns.tld = "vagrant.dev"

  # CentOS 7
  config.vm.define "ROLENAME-centos7" do |centos7|
    centos7.vm.box = "samdoran/centos7"
    centos7.vm.hostname = "ROLENAME-centos7"
    centos7.vm.network "private_network", ip: "10.77.19.100"
    config.vm.provider "virtualbox" do |vbox|
      vbox.name = "ROLENAME-centos7"
      vbox.cpus = 1
      vbox.memory = 512
    end
  end

  config.vm.provision "ansible" do |ansible|
    ansible.playbook = "vagrant.yml"
  end

end
